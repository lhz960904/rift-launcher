node_modules
out
dist
.DS_Store
*.log

# external reference repos (cloned for analysis only; see docs/REFERENCES.md)
references/
docs/
release/

# build/ holds packaging assets (icon.icns) — keep that, ignore the rest
build/*
!build/icon.icns
